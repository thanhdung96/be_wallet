<form method="post" action="{{ path('trans_delete', {'id': tran.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tran.id) }}">
    <button class="btn">Delete</button>
</form>
